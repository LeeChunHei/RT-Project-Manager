{
	"name": "rt-project-manager",
	"displayName": "RT Project Manager",
	"description": "Extension that manages projects used in NXP i.MX RT series chip",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.38.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"*",
		"onCommand:explorer.new_inc_src",
		"onCommand:explorer.create_project",
		"onCommand:explorer.properties"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "rt-project-manager.initWorkspace",
				"title": "Init Workspace"
			},
			{
				"command": "explorer.create_project",
				"title": "Create Project"
			},
			{
				"command": "explorer.properties",
				"title": "Properties"
			},
			{
				"command": "test",
				"title": "test",
				"when": "isLinux"
			}
		],
		"menus": {
			"explorer/context": [
				{
					"command": "explorer.create_project",
					"group": "navigation",
					"when": "!explorerResourceIsFolder"
				},
				{
					"command": "explorer.properties",
					"when": "explorerResourceIsFolder",
					"group": "7_modification"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.38.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"typescript": "^3.3.1",
		"tslint": "^5.12.1",
		"vscode-test": "^1.2.0"
	}
}
